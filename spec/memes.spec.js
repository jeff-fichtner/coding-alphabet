
const isEven = require('../exercises/js/memes.js');

describe("1", () => {
    it("is not even", () => {
      expect(isEven(1)).toBe(false);
    });
  });

describe("2", () => {
    it("is even", () => {
      expect(isEven(2)).toBe(true);
    });
  });

describe("3", () => {
    it("is not even", () => {
      expect(isEven(3)).toBe(false);
    });
  });

describe("4", () => {
    it("is even", () => {
      expect(isEven(4)).toBe(true);
    });
  });

describe("5", () => {
    it("is not even", () => {
      expect(isEven(5)).toBe(false);
    });
  });

describe("6", () => {
    it("is even", () => {
      expect(isEven(6)).toBe(true);
    });
  });

describe("7", () => {
    it("is not even", () => {
      expect(isEven(7)).toBe(false);
    });
  });

describe("8", () => {
    it("is even", () => {
      expect(isEven(8)).toBe(true);
    });
  });